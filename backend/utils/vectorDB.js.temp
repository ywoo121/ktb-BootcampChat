const axios = require('axios');
const { vectorDbEndpoint } = require('../config/keys');

exports.storeDocument = async (text) => {
  // 벡터화 및 저장 로직 구현 (예: OpenAI Embeddings 사용)
};

exports.searchDocuments = async (query) => {
  // 벡터 DB에서 문서 검색 로직 구현
  return [];
};